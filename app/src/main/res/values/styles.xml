<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary_light</item>
        <item name="android:windowBackground">@color/primary_lighter</item>
        <item name="android:id">@color/white</item>
        <item name="colorAccent">@color/lightGreen</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:Button">@color/lightBrown</item>
    </style>

    <style name="Widget.BottomNavigationView"
        parent="Widget.Design.BottomNavigationView">
        <item name="fontFamily">@font/countryside</item>
    </style>




</resources>
